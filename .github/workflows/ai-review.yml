name: AI Reviewer

on:
  push:
    branches:
      - "draft/**"

permissions:
  contents: write
  pull-requests: write

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install anthropic

      - name: Run AI Reviewer
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          echo "🔎 Checking for changes..."

          # 変更ファイルを特定（削除されたファイルは除外）
          CHANGED_FILES=$(git diff --name-only --diff-filter=d HEAD^ HEAD | grep '\.md$' || true)

          if [ -z "$CHANGED_FILES" ]; then
            echo "⚠️ No markdown files changed."
            echo "HAS_CHANGES=false" >> $GITHUB_ENV
            exit 0
          fi

          echo "✅ Found markdown files: $CHANGED_FILES"
          echo "HAS_CHANGES=true" >> $GITHUB_ENV

          # Pythonスクリプトを実行
          python ai_reviewer.py $CHANGED_FILES

      - name: Create Pull Request
        if: env.HAS_CHANGES == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 念のためGitレベルでも差分確認
          if git diff --quiet; then
            echo "No changes made by AI."
            exit 0
          fi

          BRANCH_NAME="ai-review/${{ github.ref_name }}-$(date +%s)"

          git config --global user.name "Claude-Reviewer"
          git config --global user.email "claude@example.com"

          git checkout -b $BRANCH_NAME
          git add .
          git commit -m "refactor: AIによる記事校正"
          git push origin $BRANCH_NAME

          gh pr create \
            --base ${{ github.ref_name }} \
            --head $BRANCH_NAME \
            --title "🤖 AI Review: ${{ github.ref_name }} の校正提案" \
            --body "Claude 3.5 Sonnetによる自動レビュー結果です。内容を確認してマージ、または修正してください。"
